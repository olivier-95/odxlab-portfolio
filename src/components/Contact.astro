---
import { Mail, Send } from 'lucide-astro';
---

<section id="contact" class="py-20 lg:py-32 bg-light">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8 max-w-7xl">
    <!-- Section Header -->
    <div class="text-center max-w-3xl mx-auto mb-16">
      <h2
        class="text-3xl sm:text-4xl lg:text-5xl font-bold text-dark mb-6 leading-tight"
      >
        Let's Work Together
      </h2>
      <p class="text-lg text-gray-700 leading-relaxed">
        Lorem ipsum dolor sit amet, consectetur adipiscing elit. Have a project
        in mind? I'd love to hear about it. Drop me a message and let's create
        something amazing together.
      </p>
    </div>

    <div class="grid lg:grid-cols-2 gap-12 max-w-6xl mx-auto">
      <!-- Contact Form -->
      <div class="bg-white rounded-2xl p-8 lg:p-10 shadow-sm border border-gray-100">
        <form
          action="https://formspree.io/f/YOUR_FORM_ID"
          method="POST"
          class="space-y-6"
        >
          <!-- Name Field -->
          <div>
            <label
              for="name"
              class="block text-sm font-semibold text-dark mb-2"
            >
              Name *
            </label>
            <input
              type="text"
              id="name"
              name="name"
              required
              class="w-full px-4 py-3 bg-light border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent transition-all"
              placeholder="Your name"
            />
          </div>

          <!-- Email Field -->
          <div>
            <label
              for="email"
              class="block text-sm font-semibold text-dark mb-2"
            >
              Email *
            </label>
            <input
              type="email"
              id="email"
              name="email"
              required
              class="w-full px-4 py-3 bg-light border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent transition-all"
              placeholder="your@email.com"
            />
          </div>

          <!-- Message Field -->
          <div>
            <label
              for="message"
              class="block text-sm font-semibold text-dark mb-2"
            >
              Message *
            </label>
            <textarea
              id="message"
              name="message"
              required
              rows="5"
              class="w-full px-4 py-3 bg-light border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent transition-all resize-none"
              placeholder="Tell me about your project..."
            ></textarea>
          </div>

          <!-- Submit Button -->
          <button
            type="submit"
            class="w-full px-6 py-4 bg-primary text-white rounded-lg font-semibold hover:bg-primary/90 hover:shadow-colorful transition-all hover:scale-105 inline-flex items-center justify-center gap-2 group"
          >
            <span>Send Message</span>
            <Send
              class="w-5 h-5 group-hover:translate-x-1 transition-transform"
            />
          </button>

          <!-- Form Note -->
          <p class="text-sm text-gray-500 text-center">
            I typically respond within 24 hours
          </p>
        </form>
      </div>

      <!-- Contact Info -->
      <div class="flex flex-col justify-center">
        <!-- Email Card -->
        <div
          class="bg-gradient-to-br from-primary to-secondary rounded-2xl p-8 lg:p-10 text-white mb-6"
        >
          <div
            class="w-14 h-14 bg-white/20 rounded-xl flex items-center justify-center mb-6"
          >
            <Mail class="w-7 h-7" />
          </div>
          <h3 class="text-2xl font-bold mb-3">Email Me Directly</h3>
          <p class="text-white/90 mb-4 leading-relaxed">
            Prefer email? Feel free to reach out directly at:
          </p>
          <a
            href="mailto:hello@odxlab.com"
            class="text-xl font-semibold hover:underline inline-flex items-center gap-2"
          >
            hello@odxlab.com
          </a>
        </div>

        <!-- Additional Info -->
        <div class="space-y-4 text-gray-700">
          <p class="leading-relaxed">
            <strong>Location:</strong> Your City, Country
          </p>
          <p class="leading-relaxed">
            <strong>Availability:</strong> Currently accepting new projects
          </p>
          <p class="leading-relaxed">
            <strong>Response Time:</strong> Within 24 hours on business days
          </p>
        </div>
      </div>
    </div>
  </div>
</section>
